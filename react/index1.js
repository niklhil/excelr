import React from 'react';
import ReactDOM from 'react-dom';
import MyComponent from './MyComponent';

ReactDOM.render(<MyComponent />, document.getElementById('testRoot'));






















/*

import React from 'react';: Brings in the React library, enabling you to create and define components, manage state, handle lifecycle methods, and use JSX.

import ReactDOM from 'react-dom';: Brings in the ReactDOM library, providing methods to render React components to the DOM, hydrate server-rendered components, and create portals.

Here render() method is used to render react component into DOM element.
*/